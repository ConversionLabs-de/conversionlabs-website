title: Article
sections:
  main:
    display: Main
    fields:
      -
        import: resource_base
        config:
          title:
            required: true
  sidebar:
    display: Sidebar
    fields:
      -
        handle: slug
        field:
          type: slug
          localizable: true
